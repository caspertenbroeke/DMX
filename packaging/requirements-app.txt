# Alles wat de ingepakte app (Windows / macOS / Linux) gebruikt.
numpy>=1.24
pyserial>=3.5
sounddevice>=0.4.6
qrcode>=7.4
mido>=1.3
python-rtmidi>=1.5
pywebview>=5.0 ; sys_platform == "win32" or sys_platform == "darwin"
